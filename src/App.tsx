import ThemeConfig from '@/theme/ThemeConfig';
import RouterConfig from '@/routes/RouterConfig';

const App = () => {
  return (
    <ThemeConfig>
      <RouterConfig />
    </ThemeConfig>
  );
};

export default App;
